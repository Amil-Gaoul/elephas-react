import { Meta, Description, Props, Preview, Story } from '@storybook/addon-docs/blocks';

import { Textarea } from './';

<Meta title="Components|Textarea" component={Textarea} />

# Textarea

<Props of={Textarea} />

<Preview>
  <form>
   <style dangerouslySetInnerHTML={{__html: `
      .input {
        margin-bottom: 16px;
      }
    `}} />
    <Textarea
      appearance="default"
      className="input"
      label="Комментарий"
      value="Съешь еще этих мягких французских булок"
      width="8"
    />
    <Textarea
      appearance="default"
      className="input"
      label="Комментарий"
      width="8"
      rows="6"
    />
    <Textarea
      className="input"
      label="Ваши комментарии, замечания и пожелания напишите в этом поле"
      width="8"
      rows="6"
    />
  </form>
</Preview>
